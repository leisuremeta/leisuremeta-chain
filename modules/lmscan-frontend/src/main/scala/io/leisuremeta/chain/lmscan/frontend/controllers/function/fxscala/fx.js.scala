// package io.leisuremeta.chain.lmscan.frontend.controllers.function.fxscala

// js
// const go = (...args) => reduce((a,f) => f(a),args)
// const pipe = (...fs) => a => go(a,...fs)

// def go[A](xs: Seq[A]) = reduce2[A, A => A]((a: A, f: A => A) => f(a), xs)
